# Example environment configuration for the Addicted dashboard
#
# Copy this file to `.env` or `.env.local` and replace the placeholder values
# with your actual API keys and RPC endpoints.  Do not commit your
# sensitive keys to version control.

# Solana RPC endpoint used by getTokenSupply().  If you have a
# dedicated provider (e.g., Helius, Triton), set this to your custom
# endpoint.  Otherwise, the default public endpoint will be used.
SOLANA_RPC_ENDPOINT=https://broken-nameless-forest.solana-mainnet.quiknode.pro/dd7817e73ddbd1cc3f8b4a6faa30068e7091a151

# Jupiter price API base URL.  For free usage, leave this at the
# default (`https://price.jup.ag/v4/price`).  For paid plans, you may
# need to use `https://api.jup.ag/price` or `https://api.jup.ag/**`.
JUPITER_API_URL=https://lite-api.jup.ag/price/v3

# Optional: Jupiter API key.  Leave empty if using the free tier.  If
# you subscribe to a paid tier on portal.jup.ag, provide your key
# here.  It will be sent in the `x-api-key` header when requesting
# prices【699046340425561†L48-L63】.
# JUPITER_API_KEY=

# Optional: Helius API key used for retrieving mint and burn events.
# You can sign up at https://helius.xyz/ to obtain a free or paid key.
# Without this key, getMintBurnEvents() will return null.
# HELIUS_API_KEY=